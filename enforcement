<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>North West Atlas | Asset Recovery & Cross-Border Enforcement</title>
    <meta name="description" content="Institutional framework for cross-border asset tracing, freezing orders, and recovery through Cayman Islands Grand Court and offshore jurisdictions.">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,300;0,400;0,500;1,300;1,400&family=Playfair+Display:wght@200;300;400&family=Inter:wght@200;300;400&display=swap" rel="stylesheet">

    <style>
        :root {
            --gold: #C5A059;
            --gold-light: #DFC896;
            --gold-dark: #8B6F4C;
            --gold-rgb: 197, 160, 89;

            --bg-deep: #070708;
            --bg-surface: #0D0D10;
            --bg-elevated: #131316;

            --text-primary: #EDE9E3;
            --text-secondary: #9FA6B4;
            --text-muted: #6B7280;

            --border: rgba(197, 160, 89, 0.06);
            --border-hover: rgba(197, 160, 89, 0.14);

            --font-primary: 'EB Garamond', Georgia, serif;
            --font-display: 'Playfair Display', Georgia, serif;
            --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;

            --ease: cubic-bezier(0.25, 0.1, 0.25, 1);
            --ease-out: cubic-bezier(0.0, 0, 0.2, 1);
            --ease-power: cubic-bezier(0.16, 1, 0.3, 1);

            --radius-sm: 2px;
            --radius-md: 8px;

            --space-xs: 0.5rem;
            --space-sm: 1rem;
            --space-md: 2rem;
            --space-lg: 3rem;
            --space-xl: 5rem;
            --space-2xl: 8rem;
            --space-3xl: 13rem;
        }

        *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

        html {
            scroll-behavior: smooth;
            scroll-padding-top: 80px;
            background: var(--bg-deep);
        }

        body {
            font-family: var(--font-primary);
            font-size: 17px;
            line-height: 1.72;
            letter-spacing: 0.01em;
            color: var(--text-secondary);
            background: var(--bg-deep);
            min-height: 100vh;
            font-weight: 300;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            overflow-x: hidden;
            -webkit-user-select: none;
            -moz-user-select: none;
            user-select: none;
            -webkit-touch-callout: none;
        }

        input, textarea, select { 
            -webkit-user-select: text; 
            -moz-user-select: text; 
            user-select: text; 
        }

        ::selection {
            background: rgba(var(--gold-rgb), 0.16);
            color: var(--text-primary);
        }

        p { max-width: 56ch; }

        h1, h2, h3, h4 {
            font-family: var(--font-display);
            font-weight: 300;
            color: var(--text-primary);
            line-height: 1.08;
            letter-spacing: -0.03em;
        }

        h2 { font-size: clamp(2.2rem, 4.5vw, 3.4rem); }
        h3 { font-size: clamp(1.4rem, 2.2vw, 1.8rem); }

        /* ═══ ENTRANCE ═══ */
        .page-veil {
            position: fixed;
            inset: 0;
            background: var(--bg-deep);
            z-index: 9999;
            pointer-events: none;
            opacity: 1;
            transition: opacity 1.6s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .page-veil.lifted { opacity: 0; }

        .scroll-progress {
            position: fixed;
            top: 0; left: 0;
            width: 0%;
            height: 1px;
            background: var(--gold);
            z-index: 200;
            opacity: 0.2;
        }

        /* ═══ LABELS ═══ */
        .section-label {
            font-family: var(--font-sans);
            font-size: 0.6rem;
            font-weight: 400;
            letter-spacing: 0.45em;
            text-transform: uppercase;
            color: var(--gold);
            margin-bottom: var(--space-sm);
            display: inline-flex;
            align-items: center;
            gap: 0.8rem;
            opacity: 0.8;
        }

        .section-label::before {
            content: '';
            width: 24px;
            height: 1px;
            background: var(--gold);
            opacity: 0.4;
        }

        .gold-text {
            background: linear-gradient(135deg, var(--gold-light), var(--gold), var(--gold-dark));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* ═══ SKIP / A11Y ═══ */
        .skip-link {
            position: absolute;
            top: -100%;
            left: var(--space-md);
            background: var(--gold);
            color: var(--bg-deep);
            padding: var(--space-xs) var(--space-sm);
            font-size: 0.8rem;
            font-weight: 600;
            z-index: 200;
            border-radius: 0 0 4px 4px;
            text-decoration: none;
            transition: top 0.3s ease;
        }
        .skip-link:focus { top: 0; }

        .sr-only {
            position: absolute;
            width: 1px; height: 1px;
            padding: 0; margin: -1px;
            overflow: hidden;
            clip: rect(0,0,0,0);
            white-space: nowrap; border: 0;
        }

        a:focus-visible, button:focus-visible {
            outline: 2px solid rgba(var(--gold-rgb), 0.4);
            outline-offset: 4px;
        }

        /* ═══ AMBIENT — consistent with main site ═══ */
        .ambient-bg {
            position: fixed;
            inset: 0;
            z-index: -2;
            background: var(--bg-deep);
        }

        .ambient-bg::after {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.3) 100%);
            z-index: 1;
            pointer-events: none;
        }

        .ambient-orb {
            position: absolute;
            animation: orbFloat 28s ease-in-out infinite;
        }

        .orb-inner {
            width: 100%; height: 100%;
            border-radius: 50%;
            filter: blur(90px);
        }

        .ambient-orb-1 {
            width: 600px; height: 600px;
            top: -10%; right: -5%;
        }
        .ambient-orb-1 .orb-inner {
            background: radial-gradient(circle, rgba(var(--gold-rgb), 0.04) 0%, transparent 65%);
        }

        .ambient-orb-2 {
            width: 450px; height: 450px;
            bottom: -12%; left: -8%;
            animation-delay: -14s;
        }
        .ambient-orb-2 .orb-inner {
            background: radial-gradient(circle, rgba(var(--gold-rgb), 0.02) 0%, transparent 65%);
        }

        @keyframes orbFloat {
            0%, 100% { transform: translate(0, 0) scale(1); }
            33% { transform: translate(20px, -15px) scale(1.03); }
            66% { transform: translate(-12px, 18px) scale(0.97); }
        }

        .noise-overlay {
            position: fixed;
            inset: 0;
            z-index: -1;
            opacity: 0.015;
            pointer-events: none;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='1'/%3E%3C/svg%3E");
            background-repeat: repeat;
            background-size: 256px;
        }

        /* ═══ CONTAINER ═══ */
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 var(--space-lg);
        }

        /* ═══ HEADER — matches main site ═══ */
        header {
            position: fixed;
            top: 0; left: 0; width: 100%;
            z-index: 100;
            padding: 1.4rem 0;
            transition: all 1s var(--ease);
        }

        header.scrolled {
            background: rgba(7,7,8,0.92);
            backdrop-filter: blur(50px) saturate(180%);
            -webkit-backdrop-filter: blur(50px) saturate(180%);
            padding: 0.5rem 0;
            border-bottom: 1px solid rgba(var(--gold-rgb), 0.03);
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            text-decoration: none;
            flex-shrink: 0;
        }

        .logo img {
            height: 36px;
            width: auto;
            filter: drop-shadow(0 0 16px rgba(var(--gold-rgb), 0.05));
            transition: all 1s var(--ease);
            transform-origin: left center;
        }

        header.scrolled .logo img { transform: scale(0.9); }

        .logo:hover img {
            filter: drop-shadow(0 0 24px rgba(var(--gold-rgb), 0.09));
        }

        .nav-menu {
            display: flex;
            gap: clamp(1.8rem, 3vw, 3.5rem);
            list-style: none;
            align-items: center;
        }

        .nav-link {
            font-family: var(--font-sans);
            font-size: 0.6rem;
            font-weight: 400;
            letter-spacing: 0.22em;
            text-transform: uppercase;
            color: var(--text-muted);
            text-decoration: none;
            transition: color 0.5s var(--ease);
            position: relative;
            padding: 0.4em 0;
            white-space: nowrap;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: 0; left: 50%;
            width: 0; height: 1px;
            background: var(--gold);
            transition: all 0.5s var(--ease);
            transform: translateX(-50%);
            opacity: 0.6;
        }

        .nav-link:hover { color: var(--text-primary); }
        .nav-link:hover::after { width: 100%; }
        .nav-link.active { color: var(--gold); }
        .nav-link.active::after { width: 100%; opacity: 0.3; }

        .nav-sep {
            width: 2px; height: 2px;
            border-radius: 50%;
            background: var(--gold);
            opacity: 0.12;
            flex-shrink: 0;
        }

        .nav-back {
            font-family: var(--font-sans);
            font-size: 0.55rem;
            font-weight: 300;
            letter-spacing: 0.15em;
            text-transform: uppercase;
            color: var(--text-muted);
            text-decoration: none;
            opacity: 0.4;
            transition: all 0.5s var(--ease);
            display: inline-flex;
            align-items: center;
            gap: 0.4rem;
        }

        .nav-back:hover { opacity: 1; color: var(--gold); }

        .nav-back svg {
            width: 10px; height: 10px;
            stroke: currentColor;
            fill: none;
            stroke-width: 1.5;
        }

        .menu-toggle {
            display: none;
            background: none; border: none;
            cursor: pointer;
            padding: var(--space-xs);
            z-index: 101;
        }

        .menu-toggle svg {
            width: 22px; height: 22px;
            stroke: var(--gold);
            stroke-width: 1.5;
            fill: none;
        }

        @media (max-width: 992px) {
            .nav-menu {
                position: fixed;
                inset: 0;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                gap: var(--space-lg);
                background: rgba(7,7,8,0.98);
                backdrop-filter: blur(60px);
                opacity: 0;
                pointer-events: none;
                transition: opacity 0.5s var(--ease);
                z-index: 99;
            }
            .nav-menu.open { opacity: 1; pointer-events: auto; }
            .nav-sep { display: none; }
            .menu-toggle { display: block; }
        }

        /* ═══ HERO — clear, institutional ═══ */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
            padding: var(--space-3xl) 0 var(--space-2xl);
        }

        .hero-content { width: 100%; }

        .hero-jurisdiction {
            font-family: var(--font-sans);
            font-size: 0.52rem;
            letter-spacing: 0.5em;
            text-transform: uppercase;
            color: var(--text-muted);
            opacity: 0.4;
            margin-bottom: var(--space-md);
        }

        .hero h1 {
            font-family: var(--font-display);
            font-size: clamp(3.2rem, 7vw, 6rem);
            line-height: 0.95;
            font-weight: 200;
            letter-spacing: -0.04em;
            margin-bottom: var(--space-sm);
        }

        .hero-purpose {
            font-family: var(--font-sans);
            font-size: 0.65rem;
            letter-spacing: 0.4em;
            text-transform: uppercase;
            color: var(--gold);
            opacity: 0.7;
            margin-bottom: var(--space-lg);
            font-weight: 400;
        }

        .hero-desc {
            font-family: var(--font-primary);
            font-size: 1.15rem;
            font-weight: 300;
            font-style: italic;
            color: var(--text-secondary);
            max-width: 460px;
            border-left: 2px solid rgba(var(--gold-rgb), 0.12);
            padding-left: var(--space-md);
            line-height: 1.9;
            opacity: 0.8;
        }

        .hero-scroll {
            position: absolute;
            bottom: var(--space-xl);
            left: var(--space-lg);
            font-family: var(--font-sans);
            font-size: 0.48rem;
            letter-spacing: 0.6em;
            text-transform: uppercase;
            color: var(--text-muted);
            transform: rotate(-90deg) translateX(-50%);
            transform-origin: left;
            opacity: 0.18;
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }

        .hero-scroll::after {
            content: '';
            width: 28px;
            height: 1px;
            background: var(--gold);
            opacity: 0.3;
            animation: scrollPulse 2.5s ease-in-out infinite;
        }

        @keyframes scrollPulse {
            0%, 100% { width: 18px; opacity: 0.15; }
            50% { width: 34px; opacity: 0.4; }
        }

        .hero-fade {
            opacity: 0;
            transform: translateY(20px);
            animation: heroIn 1.8s var(--ease-power) forwards;
        }
        .hero-fade-1 { animation-delay: 0.4s; }
        .hero-fade-2 { animation-delay: 0.7s; }
        .hero-fade-3 { animation-delay: 1.0s; }
        .hero-fade-4 { animation-delay: 1.3s; }

        @keyframes heroIn {
            to { opacity: 1; transform: translateY(0); }
        }

        @media (prefers-reduced-motion: reduce) {
            .hero-fade { opacity: 1; transform: none; animation: none; }
        }

        /* ═══ STATS BAR ═══ */
        .stats-section {
            position: relative;
            border-top: 1px solid var(--border);
            border-bottom: 1px solid var(--border);
        }

        .stats-section::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(180deg, rgba(0,0,0,0.25) 0%, rgba(0,0,0,0.1) 50%, rgba(0,0,0,0.25) 100%);
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: var(--space-xl);
            position: relative;
        }

        .stat-item {
            text-align: center;
            padding: var(--space-lg) 0;
            position: relative;
        }

        .stat-item::after {
            content: '';
            position: absolute;
            right: 0;
            top: 22%;
            height: 56%;
            width: 1px;
            background: linear-gradient(180deg, transparent, rgba(var(--gold-rgb), 0.06), transparent);
        }
        .stat-item:last-child::after { display: none; }

        .stat-number {
            font-family: var(--font-display);
            font-size: clamp(2.4rem, 4vw, 3.6rem);
            font-weight: 200;
            color: var(--gold);
            line-height: 1;
            margin-bottom: 0.5rem;
            letter-spacing: -0.03em;
        }

        .stat-label {
            font-family: var(--font-sans);
            font-size: 0.5rem;
            letter-spacing: 0.38em;
            text-transform: uppercase;
            color: var(--text-muted);
            font-weight: 300;
        }

        .stats-disclaimer {
            font-family: var(--font-sans);
            font-size: 0.5rem;
            color: var(--text-muted);
            text-align: center;
            padding: var(--space-sm) 0;
            opacity: 0.35;
            position: relative;
        }

        /* ═══ DIVIDERS ═══ */
        .gold-rule {
            border: none;
            height: 1px;
            background: linear-gradient(90deg, transparent 2%, rgba(var(--gold-rgb), 0.1) 30%, rgba(var(--gold-rgb), 0.1) 70%, transparent 98%);
            margin: 0;
            position: relative;
        }

        .gold-rule::after {
            content: '◆';
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            font-size: 0.35rem;
            color: var(--gold);
            opacity: 0.1;
            background: var(--bg-deep);
            padding: 0 0.8rem;
            line-height: 1;
        }

        /* ═══ PROTOCOLS — 1px grid ═══ */
        .protocols-section {
            padding: var(--space-2xl) 0;
        }

        .protocols-header {
            margin-bottom: var(--space-xl);
        }

        .protocols-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1px;
            background: rgba(var(--gold-rgb), 0.03);
            border-radius: var(--radius-md);
            overflow: hidden;
        }

        .protocol-card {
            background: var(--bg-surface);
            padding: var(--space-xl) var(--space-lg);
            transition: background 0.6s var(--ease);
            cursor: default;
            position: relative;
        }

        .protocol-card::after {
            content: '';
            position: absolute;
            top: 0; left: 15%; right: 15%;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--gold), transparent);
            opacity: 0;
            transition: opacity 0.6s var(--ease);
        }

        .protocol-card:hover { background: rgba(13,13,16,0.98); }
        .protocol-card:hover::after { opacity: 0.3; }

        .protocol-number {
            font-family: var(--font-sans);
            font-size: 0.6rem;
            color: var(--gold);
            opacity: 0.12;
            letter-spacing: 0.2em;
            margin-bottom: var(--space-md);
            font-weight: 300;
        }

        .protocol-card h3 {
            font-size: 1.3rem;
            margin-bottom: var(--space-sm);
        }

        .protocol-card p {
            font-size: 0.86rem;
            color: var(--text-muted);
            line-height: 1.8;
        }

        .protocol-timeline {
            font-family: var(--font-sans);
            font-size: 0.5rem;
            color: var(--text-muted);
            opacity: 0.4;
            margin-top: var(--space-md);
            padding-top: var(--space-sm);
            border-top: 1px solid var(--border);
            letter-spacing: 0.15em;
            text-transform: uppercase;
            font-weight: 300;
        }

        /* ═══ LIFECYCLE ═══ */
        .lifecycle-section {
            padding: var(--space-2xl) 0;
            border-top: 1px solid var(--border);
            border-bottom: 1px solid var(--border);
            position: relative;
        }

        .lifecycle-section::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(180deg, rgba(0,0,0,0.2) 0%, transparent 30%, transparent 70%, rgba(0,0,0,0.2) 100%);
        }

        .lifecycle-header {
            margin-bottom: var(--space-xl);
            position: relative;
        }

        .lifecycle-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 1px;
            background: rgba(var(--gold-rgb), 0.03);
            border-radius: var(--radius-md);
            overflow: hidden;
            position: relative;
        }

        .phase-card {
            background: var(--bg-surface);
            padding: var(--space-xl) var(--space-md);
            text-align: center;
            transition: background 0.6s var(--ease);
        }

        .phase-card:hover { background: rgba(13,13,16,0.98); }

        .phase-number {
            font-family: var(--font-sans);
            font-size: 0.5rem;
            letter-spacing: 0.3em;
            text-transform: uppercase;
            color: var(--gold);
            opacity: 0.4;
            margin-bottom: var(--space-sm);
            font-weight: 300;
        }

        .phase-title {
            font-family: var(--font-display);
            font-size: 1.1rem;
            font-weight: 300;
            color: var(--text-primary);
            margin-bottom: var(--space-sm);
        }

        .phase-desc {
            font-size: 0.82rem;
            color: var(--text-muted);
            line-height: 1.8;
        }

        .phase-duration {
            font-family: var(--font-sans);
            font-size: 0.48rem;
            color: var(--text-muted);
            opacity: 0.35;
            margin-top: var(--space-md);
            letter-spacing: 0.15em;
            text-transform: uppercase;
            font-weight: 300;
        }

        /* ═══ CALCULATOR — institutional, not SaaS ═══ */
        .calc-section {
            padding: var(--space-2xl) 0;
        }

        .calc-header {
            margin-bottom: var(--space-xl);
        }

        .calc-header p {
            color: var(--text-muted);
            font-size: 0.88rem;
            margin-top: var(--space-sm);
            max-width: 480px;
        }

        .calc-layout {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1px;
            background: rgba(var(--gold-rgb), 0.03);
            border-radius: var(--radius-md);
            overflow: hidden;
        }

        .calc-inputs {
            background: var(--bg-surface);
            padding: var(--space-xl) var(--space-lg);
        }

        .control-group {
            margin-bottom: var(--space-lg);
        }

        .control-label {
            font-family: var(--font-sans);
            font-size: 0.55rem;
            letter-spacing: 0.3em;
            text-transform: uppercase;
            color: var(--gold);
            opacity: 0.6;
            margin-bottom: var(--space-xs);
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-weight: 300;
        }

        .input-text {
            width: 100%;
            background: rgba(255,255,255, 0.02);
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            color: var(--text-primary);
            font-family: var(--font-primary);
            font-size: 1.2rem;
            padding: 0.75rem 1rem;
            outline: none;
            transition: all 0.4s var(--ease);
        }

        .input-text:focus {
            border-color: rgba(var(--gold-rgb), 0.4);
            background: rgba(var(--gold-rgb), 0.015);
        }

        .select-custom {
            width: 100%;
            background: rgba(255,255,255, 0.02);
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            color: var(--text-primary);
            font-family: var(--font-primary);
            font-size: 1.05rem;
            padding: 0.75rem 1rem;
            outline: none;
            cursor: pointer;
            transition: all 0.4s var(--ease);
            -webkit-appearance: none;
            appearance: none;
            background-image: url("data:image/svg+xml,%3Csvg width='10' height='6' viewBox='0 0 10 6' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 1l4 4 4-4' stroke='%236B7280' stroke-width='1.5' stroke-linecap='round'/%3E%3C/svg%3E");
            background-repeat: no-repeat;
            background-position: right 1rem center;
        }

        .select-custom option {
            background: var(--bg-surface);
            color: var(--text-primary);
        }

        .select-custom:focus { border-color: rgba(var(--gold-rgb), 0.4); }

        .range-container { margin-top: var(--space-xs); }

        .range-track {
            -webkit-appearance: none;
            width: 100%;
            height: 2px;
            background: rgba(var(--gold-rgb), 0.1);
            border-radius: 1px;
            outline: none;
        }

        .range-track::-webkit-slider-thumb {
            -webkit-appearance: none;
            width: 16px; height: 16px;
            background: var(--gold);
            border-radius: 50%;
            cursor: pointer;
            border: 3px solid var(--bg-deep);
            box-shadow: 0 0 12px rgba(var(--gold-rgb), 0.15);
        }

        .range-track::-moz-range-thumb {
            width: 16px; height: 16px;
            background: var(--gold);
            border-radius: 50%;
            cursor: pointer;
            border: 3px solid var(--bg-deep);
        }

        .slider-value {
            font-family: var(--font-sans);
            font-size: 0.85rem;
            color: var(--text-primary);
            font-weight: 300;
        }

        .btn-compute {
            background: transparent;
            border: 1px solid rgba(var(--gold-rgb), 0.2);
            color: var(--gold);
            padding: 0.9rem 2rem;
            font-family: var(--font-sans);
            text-transform: uppercase;
            font-size: 0.55rem;
            letter-spacing: 0.25em;
            cursor: pointer;
            transition: all 0.5s var(--ease);
            width: 100%;
            border-radius: var(--radius-sm);
            font-weight: 300;
            margin-top: var(--space-sm);
        }

        .btn-compute:hover {
            border-color: rgba(var(--gold-rgb), 0.5);
            background: rgba(var(--gold-rgb), 0.04);
        }

        /* Result panel — clean, textual */
        .calc-result {
            background: var(--bg-surface);
            padding: var(--space-xl) var(--space-lg);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            transition: background 0.6s var(--ease);
        }

        .result-waiting {
            opacity: 0.15;
        }

        .result-waiting-text {
            font-family: var(--font-sans);
            font-size: 0.52rem;
            letter-spacing: 0.3em;
            text-transform: uppercase;
            color: var(--text-muted);
        }

        .result-active { display: none; }

        .result-number {
            font-family: var(--font-display);
            font-size: clamp(4rem, 8vw, 6rem);
            font-weight: 200;
            color: var(--gold);
            line-height: 1;
            letter-spacing: -0.04em;
            margin-bottom: var(--space-xs);
        }

        .result-unit {
            font-family: var(--font-sans);
            font-size: 0.5rem;
            letter-spacing: 0.4em;
            text-transform: uppercase;
            color: var(--text-muted);
            margin-bottom: var(--space-lg);
            font-weight: 300;
        }

        .result-assessment {
            font-family: var(--font-sans);
            font-size: 0.55rem;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            color: var(--text-primary);
            margin-bottom: var(--space-md);
            font-weight: 400;
        }

        .result-recommendation {
            font-family: var(--font-primary);
            font-size: 0.9rem;
            color: var(--text-muted);
            line-height: 1.8;
            max-width: 320px;
            font-style: italic;
        }

        .calc-disclaimer {
            font-family: var(--font-sans);
            font-size: 0.5rem;
            color: var(--text-muted);
            margin-top: var(--space-lg);
            opacity: 0.35;
            max-width: 580px;
            line-height: 1.9;
            font-weight: 300;
        }

        /* ═══ FOOTER ═══ */
        footer {
            background: var(--bg-deep);
            border-top: 1px solid var(--border);
            padding: var(--space-xl) 0;
        }

        .legal-disclaimer {
            padding: var(--space-md) var(--space-lg);
            background: rgba(0,0,0,0.15);
            font-family: var(--font-sans);
            font-size: 0.52rem;
            line-height: 1.85;
            color: var(--text-muted);
            border-left: 2px solid rgba(var(--gold-rgb), 0.06);
            opacity: 0.45;
            border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
            font-weight: 300;
            margin-bottom: var(--space-xl);
        }

        .legal-disclaimer a {
            color: var(--gold);
            text-decoration: underline;
            text-underline-offset: 3px;
            text-decoration-thickness: 0.5px;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: var(--space-lg);
            margin-bottom: var(--space-xl);
        }

        .footer-brand h4 {
            font-family: var(--font-display);
            font-size: 0.92rem;
            font-weight: 300;
            letter-spacing: 0.08em;
            opacity: 0.4;
            margin-bottom: var(--space-sm);
        }

        .footer-brand p {
            font-size: 0.78rem;
            color: var(--text-muted);
            line-height: 1.7;
            max-width: 300px;
            opacity: 0.6;
        }

        .footer-col h5 {
            font-family: var(--font-sans);
            font-size: 0.48rem;
            letter-spacing: 0.3em;
            text-transform: uppercase;
            color: var(--gold);
            opacity: 0.4;
            margin-bottom: var(--space-sm);
            font-weight: 300;
        }

        .footer-col a {
            display: block;
            font-size: 0.78rem;
            color: var(--text-muted);
            text-decoration: none;
            margin-bottom: 0.5rem;
            transition: color 0.4s var(--ease);
            font-weight: 300;
            opacity: 0.6;
        }
        .footer-col a:hover { color: var(--gold); opacity: 1; }

        .footer-bottom {
            padding-top: var(--space-lg);
            border-top: 1px solid var(--border);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: var(--space-md);
            font-family: var(--font-sans);
            font-size: 0.48rem;
            color: var(--text-muted);
            opacity: 0.28;
            font-weight: 300;
        }

        .footer-legal {
            display: flex;
            gap: var(--space-lg);
        }
        .footer-legal a {
            color: var(--text-muted);
            text-decoration: none;
            font-size: 0.48rem;
            letter-spacing: 0.1em;
        }

        .footer-seal {
            text-align: center;
            padding: var(--space-xl) 0 var(--space-sm);
            opacity: 0.045;
            font-size: 1.2rem;
            color: var(--gold);
            user-select: none;
        }

        /* ═══ REVEAL ═══ */
        .reveal {
            opacity: 0;
            transform: translateY(10px);
            transition: opacity 1.4s var(--ease), transform 1.4s var(--ease);
        }
        .reveal.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .reveal-stagger.visible .reveal-child {
            opacity: 1;
            transform: translateY(0);
        }

        .reveal-child {
            opacity: 0;
            transform: translateY(10px);
            transition: opacity 0.9s var(--ease), transform 0.9s var(--ease);
        }

        .reveal-stagger.visible .reveal-child:nth-child(1) { transition-delay: 0s; }
        .reveal-stagger.visible .reveal-child:nth-child(2) { transition-delay: 0.1s; }
        .reveal-stagger.visible .reveal-child:nth-child(3) { transition-delay: 0.2s; }
        .reveal-stagger.visible .reveal-child:nth-child(4) { transition-delay: 0.3s; }

        @media (prefers-reduced-motion: reduce) {
            .reveal, .reveal-child { opacity: 1; transform: none; transition: none; }
            .page-veil { display: none; }
        }

        /* ═══ RESPONSIVE ═══ */
        @media (max-width: 1200px) {
            .lifecycle-grid { grid-template-columns: repeat(2, 1fr); }
        }

        @media (max-width: 992px) {
            .protocols-grid { grid-template-columns: 1fr; }
            .calc-layout { grid-template-columns: 1fr; }
            .stats-grid { grid-template-columns: repeat(2, 1fr); }
            .stat-item::after { display: none; }
            .footer-grid { grid-template-columns: 1fr 1fr; }
        }

        @media (max-width: 768px) {
            .container { padding: 0 var(--space-md); }
            .hero-scroll { display: none; }
            .lifecycle-grid { grid-template-columns: 1fr; }
            .scroll-progress { display: none; }
            .footer-grid { grid-template-columns: 1fr; }
        }

        @media (max-width: 480px) {
            .stats-grid { grid-template-columns: 1fr; }
        }

        @media print {
            .ambient-bg, .noise-overlay, .page-veil, .scroll-progress, .hero-scroll { display: none; }
            body { background: white; color: black; }
        }
    </style>
</head>
<body>
    <div class="page-veil" id="pageVeil" aria-hidden="true"></div>
    <div class="scroll-progress" id="scrollProgress" aria-hidden="true"></div>

    <a href="#main-content" class="skip-link">Skip to content</a>

    <div class="ambient-bg" aria-hidden="true">
        <div class="ambient-orb ambient-orb-1"><div class="orb-inner"></div></div>
        <div class="ambient-orb ambient-orb-2"><div class="orb-inner"></div></div>
    </div>
    <div class="noise-overlay" aria-hidden="true"></div>

    <header id="header" role="banner">
        <nav class="container" aria-label="Page navigation">
            <a href="index.html" class="logo" aria-label="North West Atlas — Home">
                <img src="logo-nwa-centered.png"
                     alt="North West Atlas B Corp"
                     width="144" height="36"
                     loading="eager" decoding="async">
            </a>

            <ul class="nav-menu" id="navMenu">
                <li><a href="#protocols" class="nav-link">Protocols</a></li>
                <li><a href="#lifecycle" class="nav-link">Process</a></li>
                <li><a href="#calculator" class="nav-link">Calculator</a></li>
                <li aria-hidden="true"><span class="nav-sep"></span></li>
                <li>
                    <a href="index.html" class="nav-back">
                        <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M19 12H5m7-7l-7 7 7 7"/></svg>
                        Main Site
                    </a>
                </li>
            </ul>

            <button class="menu-toggle" id="menuToggle"
                    aria-controls="navMenu" aria-expanded="false"
                    aria-label="Open navigation menu">
                <svg viewBox="0 0 24 24" aria-hidden="true">
                    <path d="M4 7h16M4 12h16M4 17h16" stroke="currentColor" stroke-linecap="round"/>
                </svg>
            </button>
        </nav>
    </header>

    <main id="main-content">

        <!-- ═══ HERO ═══ -->
        <section class="hero" id="hero" aria-label="Overview">
            <div class="container hero-content">
                <div class="hero-jurisdiction hero-fade hero-fade-1">
                    Cayman Islands · Grand Court Jurisdiction
                </div>

                <h1 class="hero-fade hero-fade-2">
                    Asset Recovery &amp;<br>
                    <span class="gold-text">Cross-Border Enforcement</span>
                </h1>

                <div class="hero-purpose hero-fade hero-fade-3">
                    Norwich Pharmacal · Mareva Injunctions · Forensic Reconstruction
                </div>

                <p class="hero-desc hero-fade hero-fade-4">
                    Institutional framework for tracing, freezing, and recovering 
                    misappropriated assets across offshore jurisdictions. Converting 
                    legal claims into executable outcomes.
                </p>
            </div>

            <div class="hero-scroll" aria-hidden="true">Scroll</div>
        </section>

        <!-- ═══ STATS ═══ -->
        <section class="stats-section" aria-labelledby="stats-heading">
            <div class="container">
                <h2 id="stats-heading" class="sr-only">Enforcement Statistics</h2>
                <div class="stats-grid reveal-stagger reveal">
                    <div class="stat-item reveal-child">
                        <div class="stat-number" data-count="127" data-prefix="$" data-suffix="M">$127M</div>
                        <div class="stat-label">Assets frozen worldwide*</div>
                    </div>
                    <div class="stat-item reveal-child">
                        <div class="stat-number" data-count="94" data-suffix="%">94%</div>
                        <div class="stat-label">Recovery rate*</div>
                    </div>
                    <div class="stat-item reveal-child">
                        <div class="stat-number" data-count="14">14</div>
                        <div class="stat-label">Jurisdictions covered</div>
                    </div>
                    <div class="stat-item reveal-child">
                        <div class="stat-number">&lt;72h</div>
                        <div class="stat-label">Average freeze execution</div>
                    </div>
                </div>
                <p class="stats-disclaimer">* Based on enforcement data from cases managed by NWA and affiliated counsel, 2019–2025. Past outcomes are not indicative of future results.</p>
            </div>
        </section>

        <hr class="gold-rule" aria-hidden="true">

        <!-- ═══ PROTOCOLS ═══ -->
        <section class="protocols-section" id="protocols" aria-labelledby="protocols-heading">
            <div class="container">
                <div class="protocols-header reveal">
                    <span class="section-label">Enforcement Vectors</span>
                    <h2 id="protocols-heading">Recovery <span class="gold-text">Protocols</span></h2>
                </div>

                <div class="protocols-grid reveal-stagger reveal">
                    <article class="protocol-card reveal-child">
                        <div class="protocol-number" aria-hidden="true">01</div>
                        <h3>Norwich Pharmacal</h3>
                        <p>Compulsory disclosure orders against registered agents and financial intermediaries. Piercing the corporate veil to identify Ultimate Beneficial Owners.</p>
                        <div class="protocol-timeline">Typical timeline: 4–6 weeks</div>
                    </article>
                    <article class="protocol-card reveal-child">
                        <div class="protocol-number" aria-hidden="true">02</div>
                        <h3>Mareva Injunction</h3>
                        <p>Rapid deployment of worldwide freezing orders. Securing liquidity across multiple jurisdictions simultaneously within a 72-hour operational window.</p>
                        <div class="protocol-timeline">Typical timeline: 24–72 hours</div>
                    </article>
                    <article class="protocol-card reveal-child">
                        <div class="protocol-number" aria-hidden="true">03</div>
                        <h3>Forensic Reconstruction</h3>
                        <p>Algorithmic audit of transaction flows. Elimination of arithmetic anomalies and preparation of evidence-grade financial maps for court submission.</p>
                        <div class="protocol-timeline">Typical timeline: 8–12 weeks</div>
                    </article>
                </div>
            </div>
        </section>

        <!-- ═══ LIFECYCLE ═══ -->
        <section class="lifecycle-section" id="lifecycle" aria-labelledby="lifecycle-heading">
            <div class="container">
                <div class="lifecycle-header reveal">
                    <span class="section-label">Operational Sequence</span>
                    <h2 id="lifecycle-heading">Enforcement <span class="gold-text">Lifecycle</span></h2>
                </div>

                <div class="lifecycle-grid reveal-stagger reveal">
                    <div class="phase-card reveal-child">
                        <div class="phase-number">Phase 01</div>
                        <div class="phase-title">Intelligence Gathering</div>
                        <p class="phase-desc">Corporate structure mapping, UBO identification, and preliminary asset tracing across target jurisdictions.</p>
                        <div class="phase-duration">Week 1–2</div>
                    </div>
                    <div class="phase-card reveal-child">
                        <div class="phase-number">Phase 02</div>
                        <div class="phase-title">Legal Architecture</div>
                        <p class="phase-desc">Preparation of Norwich Pharmacal applications and Mareva injunction drafts. Coordination with local counsel in each jurisdiction.</p>
                        <div class="phase-duration">Week 2–4</div>
                    </div>
                    <div class="phase-card reveal-child">
                        <div class="phase-number">Phase 03</div>
                        <div class="phase-title">Rapid Deployment</div>
                        <p class="phase-desc">Simultaneous filing across jurisdictions. Ex parte hearings and emergency freezing orders within 72-hour window.</p>
                        <div class="phase-duration">Week 4–5</div>
                    </div>
                    <div class="phase-card reveal-child">
                        <div class="phase-number">Phase 04</div>
                        <div class="phase-title">Recovery &amp; Settlement</div>
                        <p class="phase-desc">Negotiated settlement under pressure of frozen assets, or full enforcement through court-ordered liquidation and distribution.</p>
                        <div class="phase-duration">Week 5–16</div>
                    </div>
                </div>
            </div>
        </section>

        <hr class="gold-rule" aria-hidden="true">

        <!-- ═══ CALCULATOR ═══ -->
        <section class="calc-section" id="calculator" aria-labelledby="calc-heading">
            <div class="container">
                <div class="calc-header reveal">
                    <span class="section-label">Indicative Assessment</span>
                    <h2 id="calc-heading">Recovery <span class="gold-text">Probability</span></h2>
                    <p>Model enforcement probability based on jurisdiction, claim size, and documentation readiness. For indicative purposes only.</p>
                </div>

                <div class="calc-layout reveal">
                    <div class="calc-inputs">
                        <div class="control-group">
                            <div class="control-label">Target Jurisdiction</div>
                            <select class="select-custom" id="jurisdiction">
                                <option value="1.0">Cayman Islands (Tier 1)</option>
                                <option value="0.92">BVI (Tier 1)</option>
                                <option value="0.78">United Kingdom (Tier 2)</option>
                                <option value="0.65">UAE / DIFC (Tier 2)</option>
                                <option value="0.45">Unregulated / Mixed</option>
                            </select>
                        </div>

                        <div class="control-group">
                            <div class="control-label">Claim Value (USD)</div>
                            <input type="text" class="input-text" id="amount" value="5,000,000" placeholder="Enter amount">
                        </div>

                        <div class="control-group">
                            <div class="control-label">
                                <span>Documentation Readiness</span>
                                <span class="slider-value" id="sliderVal">70%</span>
                            </div>
                            <div class="range-container">
                                <input type="range" class="range-track" min="1" max="100" value="70" id="readiness">
                            </div>
                        </div>

                        <button class="btn-compute" id="computeBtn">
                            Run Assessment
                        </button>
                    </div>

                    <div class="calc-result" id="resultPanel">
                        <div id="waitingState" class="result-waiting">
                            <div class="result-waiting-text">Configure parameters</div>
                        </div>

                        <div id="resultState" class="result-active">
                            <div class="result-number" id="resultNumber">0%</div>
                            <div class="result-unit">Estimated recovery probability</div>
                            <div class="result-assessment" id="resultAssessment"></div>
                            <p class="result-recommendation" id="resultRecommendation"></p>
                        </div>
                    </div>
                </div>

                <p class="calc-disclaimer">
                    This tool provides indicative probability estimates based on historical enforcement data. Actual outcomes depend on evidence quality, counterparty behavior, and judicial discretion. This does not constitute legal advice.
                </p>
            </div>
        </section>

    </main>

    <footer role="contentinfo">
        <div class="container">
            <div class="legal-disclaimer">
                <strong>IMPORTANT NOTICE:</strong> This page is for informational purposes only and does not constitute legal advice or an offer of legal services.
                Recovery statistics are based on historical enforcement data from cases managed by NWA and its affiliated counsel between 2019–2025.
                Past recovery outcomes are not indicative of future results. All enforcement actions are subject to applicable laws and court approvals.
                North West Atlas B Corp is licensed in the UAE (License #786048).
                For complete disclosures, refer to our <a href="disclosures.html">Disclosures</a> page.
            </div>

            <div class="footer-grid">
                <div class="footer-brand">
                    <h4>North West <span class="gold-text">Atlas</span> B Corp</h4>
                    <p>Institutional advisory and structured capital for technology and real assets. Cayman Islands enforcement division.</p>
                </div>
                <div class="footer-col">
                    <h5>This Page</h5>
                    <a href="#protocols">Protocols</a>
                    <a href="#lifecycle">Process</a>
                    <a href="#calculator">Calculator</a>
                </div>
                <div class="footer-col">
                    <h5>Jurisdictions</h5>
                    <a href="#">Cayman Islands</a>
                    <a href="#">British Virgin Islands</a>
                    <a href="#">United Kingdom</a>
                    <a href="#">UAE / DIFC</a>
                </div>
                <div class="footer-col">
                    <h5>Contact</h5>
                    <a href="mailto:enforcement@northwestatlas.com">enforcement@northwestatlas.com</a>
                    <a href="index.html#contact">Main Inquiries</a>
                </div>
            </div>

            <div class="footer-bottom">
                <p>&copy; <span id="year"></span> North West Atlas B Corp. George Town, Grand Cayman.</p>
                <div class="footer-legal">
                    <a href="privacy.html">Privacy Policy</a>
                    <a href="terms.html">Terms of Use</a>
                    <a href="disclosures.html">Disclosures</a>
                </div>
            </div>

            <div class="footer-seal" aria-hidden="true">✦</div>
        </div>
    </footer>

    <script>
    (function() {
        'use strict';

        document.addEventListener('contextmenu', function(e) { e.preventDefault(); });
        document.addEventListener('copy', function(e) { e.preventDefault(); });
        document.addEventListener('cut', function(e) { e.preventDefault(); });
        document.addEventListener('dragstart', function(e) { e.preventDefault(); });
        document.addEventListener('keydown', function(e) {
            if (e.ctrlKey && /^[cxauspiCXAUSPI]$/.test(e.key)) e.preventDefault();
            if (e.key === 'F12') e.preventDefault();
            if (e.ctrlKey && e.shiftKey && /^[ijcIJC]$/.test(e.key)) e.preventDefault();
        });

        var prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

        // Veil
        var veil = document.getElementById('pageVeil');
        if (veil) {
            window.addEventListener('load', function() {
                setTimeout(function() {
                    veil.classList.add('lifted');
                    setTimeout(function() { veil.style.display = 'none'; }, 1800);
                }, prefersReduced ? 0 : 150);
            });
        }

        // Header + scroll progress
        var header = document.getElementById('header');
        var progressBar = document.getElementById('scrollProgress');
        var ticking = false;

        function onScroll() {
            var y = window.scrollY;
            header.classList.toggle('scrolled', y > 50);
            if (progressBar) {
                var h = document.documentElement.scrollHeight - window.innerHeight;
                progressBar.style.width = (h > 0 ? (y / h) * 100 : 0) + '%';
            }
            ticking = false;
        }

        window.addEventListener('scroll', function() {
            if (!ticking) { requestAnimationFrame(onScroll); ticking = true; }
        }, { passive: true });
        onScroll();

        // Mobile menu
        var toggle = document.getElementById('menuToggle');
        var menu = document.getElementById('navMenu');

        if (toggle && menu) {
            toggle.addEventListener('click', function() {
                var open = menu.classList.toggle('open');
                toggle.setAttribute('aria-expanded', String(open));
                document.body.style.overflow = open ? 'hidden' : '';
            });
            menu.querySelectorAll('a').forEach(function(l) {
                l.addEventListener('click', function() {
                    menu.classList.remove('open');
                    toggle.setAttribute('aria-expanded', 'false');
                    document.body.style.overflow = '';
                });
            });
        }

        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(function(a) {
            a.addEventListener('click', function(e) {
                var h = this.getAttribute('href');
                if (h === '#') return;
                var t = document.querySelector(h);
                if (t) { e.preventDefault(); t.scrollIntoView({ behavior: prefersReduced ? 'auto' : 'smooth' }); }
            });
        });

        // Year
        var yr = document.getElementById('year');
        if (yr) yr.textContent = new Date().getFullYear();

        // Reveal
        var reveals = document.querySelectorAll('.reveal');
        if (reveals.length && 'IntersectionObserver' in window && !prefersReduced) {
            var io = new IntersectionObserver(function(entries) {
                entries.forEach(function(e) {
                    if (e.isIntersecting) { e.target.classList.add('visible'); io.unobserve(e.target); }
                });
            }, { threshold: 0.06, rootMargin: '0px 0px -30px 0px' });
            reveals.forEach(function(el) { io.observe(el); });
        } else {
            reveals.forEach(function(el) { el.classList.add('visible'); });
        }

        // Counter animation for stats
        var statNumbers = document.querySelectorAll('.stat-number[data-count]');
        if (statNumbers.length && 'IntersectionObserver' in window && !prefersReduced) {
            var co = new IntersectionObserver(function(entries) {
                entries.forEach(function(e) {
                    if (e.isIntersecting) { countUp(e.target); co.unobserve(e.target); }
                });
            }, { threshold: 0.3 });
            statNumbers.forEach(function(el) { co.observe(el); });
        }

        function countUp(el) {
            var target = parseInt(el.getAttribute('data-count'), 10);
            var pre = el.getAttribute('data-prefix') || '';
            var suf = el.getAttribute('data-suffix') || '';
            var dur = 1800, start = performance.now();
            function tick(now) {
                var p = Math.min((now - start) / dur, 1);
                var eased = 1 - (1 - p) * (1 - p);
                el.textContent = pre + Math.round(target * eased) + suf;
                if (p < 1) requestAnimationFrame(tick);
                else el.textContent = pre + target + suf;
            }
            requestAnimationFrame(tick);
        }

        // === CALCULATOR ===
        var amountInput = document.getElementById('amount');
        if (amountInput) {
            amountInput.addEventListener('input', function() {
                var v = this.value.replace(/\D/g, '');
                if (v) this.value = parseInt(v).toLocaleString('en-US');
            });
        }

        var readiness = document.getElementById('readiness');
        var sliderVal = document.getElementById('sliderVal');
        if (readiness && sliderVal) {
            readiness.addEventListener('input', function() {
                sliderVal.textContent = this.value + '%';
            });
        }

        var computeBtn = document.getElementById('computeBtn');
        if (computeBtn) {
            computeBtn.addEventListener('click', function() {
                var waitingState = document.getElementById('waitingState');
                var resultState = document.getElementById('resultState');
                var resultNumber = document.getElementById('resultNumber');
                var resultAssessment = document.getElementById('resultAssessment');
                var resultRecommendation = document.getElementById('resultRecommendation');

                waitingState.style.display = 'none';
                resultState.style.display = 'flex';
                resultState.style.flexDirection = 'column';
                resultState.style.alignItems = 'center';

                var jFactor = parseFloat(document.getElementById('jurisdiction').value);
                var rFactor = parseInt(document.getElementById('readiness').value) / 100;
                var amountStr = document.getElementById('amount').value.replace(/,/g, '');
                var amount = parseInt(amountStr) || 0;

                var baseScore = (jFactor * 0.6) + (rFactor * 0.4);
                if (amount > 10000000) baseScore += 0.05;
                if (amount > 50000000) baseScore += 0.03;

                var finalPercent = Math.round(baseScore * 100);
                if (finalPercent > 96) finalPercent = 96;
                if (finalPercent < 15) finalPercent = 15;

                // Animate number
                if (!prefersReduced) {
                    var dur = 1800, start = performance.now();
                    function tick(now) {
                        var p = Math.min((now - start) / dur, 1);
                        var eased = 1 - (1 - p) * (1 - p);
                        resultNumber.textContent = Math.round(finalPercent * eased) + '%';
                        if (p < 1) requestAnimationFrame(tick);
                        else {
                            resultNumber.textContent = finalPercent + '%';
                            showVerdict(finalPercent, resultAssessment, resultRecommendation);
                        }
                    }
                    requestAnimationFrame(tick);
                } else {
                    resultNumber.textContent = finalPercent + '%';
                    showVerdict(finalPercent, resultAssessment, resultRecommendation);
                }
            });
        }

        function showVerdict(score, assessment, rec) {
            if (score > 75) {
                assessment.textContent = 'High Recovery Probability';
                rec.textContent = 'Recommend immediate Mareva injunction filing. High likelihood of successful asset freeze and recovery within standard timeline.';
            } else if (score > 50) {
                assessment.textContent = 'Moderate — Actionable';
                rec.textContent = 'Forensic audit recommended before filing. Additional documentation may increase recovery probability by 15–25%.';
            } else {
                assessment.textContent = 'Requires Further Intelligence';
                rec.textContent = 'Asset tracing unlikely without new intelligence. Consider Norwich Pharmacal disclosure application as preliminary step.';
            }
        }

    })();
    </script>
</body>
</html>
